## Quickstart
```shell script
$ docker-compose up -d

$ docker exec -it skeleton_php sh
```

```shell script
$ composer i

$ chown -R www-data:www-data storage/ bootstrap/
```

```shell script
$ php artisan route:scan
```
